"""Interaction API endpoints — Like, Save, Comment.

Owner: Hjl2065889707

POST /api/posts/<id>/like       — toggle like      (TODO)
POST /api/posts/<id>/save       — toggle save      (TODO)
GET  /api/posts/<id>/comments   — list comments    (TODO)
POST /api/posts/<id>/comments   — add comment      (TODO)
"""

# from flask import jsonify, request
# from flask_login import login_required, current_user
# from app import app, db
# from app.models import Post, PostLike, SavedPost, Comment
